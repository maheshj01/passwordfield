void main() {
  // testWidgets("My widget has a suffix icon", (WidgetTester tester) async {
  //   await tester.pumpWidget(PasswordField(
  //     suffixIcon: Icon(Icons.remove_red_eye),
  //   ));
  //   final passwordfieldFinder = find.byWidget(PasswordField());
  //   final iconFinder = find.byIcon(Icons.remove_red_eye);

  //   prefix0.expect(passwordfieldFinder, findsOneWidget);
  //   expect(iconFinder, findsOneWidget);
  // });
}
